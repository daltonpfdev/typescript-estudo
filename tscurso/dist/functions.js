"use strict";
const sum = (x, y) => {
    return (x + y);
};
const valueSum = sum(1, 2);
const sub = (x, y) => {
    return (x - y);
};
console.log(sub(4, 5));
const log = (message) => {
    console.log(message);
};
log("Dalton");
